# Copy to .env.local, set your API key, then restart the app. Never commit .env.local.
GEMINI_API_KEY=
GEMINI_JUDGING_MODEL=gemini-3.6-flash
# Keep the same model throughout a competition.
# Optional OpenAI fallback when no Gemini key is configured:
# OPENAI_API_KEY=
# OPENAI_JUDGING_MODEL=gpt-4o-2024-08-06
