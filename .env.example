# Copy to .env.local, set your API key, then restart the app. Never commit .env.local.
OPENAI_API_KEY=
# Keep this snapshot unchanged throughout a competition. Must support images,
# structured outputs and temperature=0 through the Responses API.
OPENAI_JUDGING_MODEL=gpt-4o-2024-08-06
